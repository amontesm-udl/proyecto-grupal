Utilizando la clase Operaciones, realizar la operación asignada.
De igual manera deberás realizar la prueba correspondiente en el archivo main.py
 
Nombre                  |   Operación
-----------------------------------------------------------------------------
Edwin                   | maximo(lista) -> Regresa el número mayor de la lista
Emmanuel                | minimo(lista) -> Regresa el número menor de la lista
Patricio                | promedio(lista) -> Regresa el promedio de los números de la lista #se realizo exitosamente
Enzo                    | ordenarLista(lista) -> Regresa la lista ordenada
Aldo                    | eliminarDuplicados(lista) -> Regresa la lista sin números duplicados
Juan Antonio            | esPar(numero) -> Regresar True si el número es par False en caso contrario
Mario Alberto           | esImpar(numero) -> Regresa True si el número es impar False en caso contrario
Sarahí                  | numeroMayor(a,b,c) -> Regresa el número mayor de los tres números recibidos 
Christian Alejandro     | generarTablaMultiplicar(numero) -> Imprime la tabla de multiplicar del número indicado
Juan Miguel             | contarPalabraEnTexto(texto,palabra) -> Regresa el número de veces que se encuentra la palabra en el texto
Leonardo                | serieFibonacci(n) -> Imprime la serie fibonacci hasta el número n
Marcos                  | reemplazarEspacios(texto, caracter) -> Regresa una cadena con el texto reemplando los espacios con el caracter
Ariadna                 | suma(n1,n2) -> Regresa la suma de los dos números
Enrique                 | resta(n1,n2) -> Regresa la resta de los dos números
Jorge Iván              | multiplica(n1,n2) -> Regresa la multiplicación de los dos números
Jorge Emilio            | division(n1,n2) -> Regresa la división de los dos números
Emmanuel Alejandro      | modulo(n1,n2) -> Regresa el módulo o resto de la división de los dos números
David                   | potencia(n1,n2) -> Regresa la potencia de elevar n1 a la potencia n2
Joseph                  | raizCuadrada(n) -> Regresa la raíz cuadrada de n
Iván de Jesús           | esPositivo(n) -> Regresa la palabra Positivo o Negativo según el valor de n